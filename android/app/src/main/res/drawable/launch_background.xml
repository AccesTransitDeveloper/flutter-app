<?xml version="1.0" encoding="utf-8"?>
<!-- Shown by the OS while the process starts, until Flutter draws its first
     frame. Solid splash backdrop so there is no flash before SplashScreen. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
